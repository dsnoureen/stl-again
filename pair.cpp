#include <bits/stdc++.h>
using namespace std;
int main()
{
    pair<string, int> person;
    string name;
    int age;
    person.first=name;
    person.second=age;
    cin >> person.first >> person.second;
    cout << person.first << " " << person.second << endl;
    return 0;
}