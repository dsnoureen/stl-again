#include <bits/stdc++.h>
using namespace std;
int main()
{
    pair<string, int> person;
    cin >> person.first >> person.second;
    string name;
    int age;
    person.first=name;
    person.second=age;
    cout << person.first << " " << person.second << endl;
    return 0;
}